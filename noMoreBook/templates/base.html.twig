<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
    {% if app.user %}
        <p>hello world</p>
        {# {{ dump(app.user) }} #}

        {# Un seul formulaire de déconnexion, bien configuré #}
        {# {% if app.user %}
            <form action="{{ path('app_logout') }}" method="post" style="display:inline;">
                <input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
                <button type="submit">Déconnexion</button>
            </form>
        {% endif %} #}
         <a href="{{ path('app_logout') }}">Logout</a>{% endif %}
         {% if not app.user and app.request.attributes.get('_route') != 'app_login' %}
            <a href="{{ path('app_login') }}">se connecter</a>
        {% endif %}

        {% block body %}{% endblock %}
    </body>
</html>
